# Checked out with LF everywhere. This repository is consumed by GitHub Actions runners, and a
# bash `run:` step whose script arrives with CRLF fails on Linux with "$'\r': command not found".
* text=auto eol=lf
